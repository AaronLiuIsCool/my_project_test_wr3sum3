# application.yml

spring:
  profiles:
    active: dev
  application:
    name: kdr-service

server:
  error:
    whitelabel:
      enabled: false

kuaidaoresume:
  common:
    sentry-dsn: ${SENTRY_DSN}
    deploy-env: ${DEPLOY:V2}
  signing-secret: ${SIGNING_SECRET}

