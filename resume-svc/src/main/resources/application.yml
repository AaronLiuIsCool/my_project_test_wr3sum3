spring:
  datasource:
    url: jdbc:mysql://localhost:3306/resume_test?useUnicode=true&characterEncoding=utf-8&serverTimezone=PST
    username: root
    password:
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate.ddl-auto: update
  data:
    rest:
      base-path: /api
server:
  port: 8082
management:
  endpoints:
    web:
      exposure:
        include: "*"
